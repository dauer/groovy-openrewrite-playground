plugins {
    id 'groovy'
    id 'java'
    id "org.openrewrite.rewrite" version "6.27.2" //  "6.27.2" Works. "6.28.0" and "7.0.3" doesn't!
}

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    implementation "org.codehaus.groovy:groovy-all:3.0.18"
}

rewrite {
    configFile = project.getRootProject().file("rewrite.yml")
    activeRecipe("dk.workbench.openrewrite.groovy")
}